{
  "name": "tokenix-contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost"
  },
  "dependencies": {
    "@nomiclabs/hardhat-ethers": "^3.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "ethers": "^6.6.0",
    "hardhat": "^2.17.0"
  }
}